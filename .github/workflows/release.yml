name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

# Default: only read. Jobs opt into write where needed.
permissions:
  contents: read

jobs:
  build:
    name: build ${{ matrix.target }}
    # GNU binaries stay on Ubuntu 22.04 (glibc 2.35). Darwin is a native
    # macos-14 (Apple Silicon) job; do not use macos-latest (can be Intel).
    runs-on: ${{ matrix.os }}
    # aarch64 (musl) is best-effort; a failure there must not block required legs.
    continue-on-error: ${{ matrix.best_effort }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
            cross: false
            best_effort: false
          - target: aarch64-unknown-linux-musl
            os: ubuntu-22.04
            cross: true
            best_effort: true
          - target: aarch64-apple-darwin
            os: macos-14
            cross: false
            best_effort: false
    steps:
      - uses: actions/checkout@v7

      - name: Install Linux build dependencies
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends pkg-config libfuse3-dev libssl-dev

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build (native)
        if: ${{ !matrix.cross }}
        run: cargo build --release --locked --target ${{ matrix.target }} --bin scorpio --bin antares

      - name: Build (cross)
        if: matrix.cross
        run: cross build --release --locked --target ${{ matrix.target }} --bin scorpio --bin antares

      - name: Package tarball + checksum
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          dist="scorpiofs-${version}-${{ matrix.target }}"
          mkdir -p "$dist"
          cp "target/${{ matrix.target }}/release/scorpio" "$dist/"
          cp "target/${{ matrix.target }}/release/antares" "$dist/"
          cp LICENSE-MIT LICENSE-APACHE README.md "$dist/"
          tar -czf "${dist}.tar.gz" "$dist"
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${dist}.tar.gz" > "${dist}.tar.gz.sha256"
          else
            shasum -a 256 "${dist}.tar.gz" > "${dist}.tar.gz.sha256"
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist-${{ matrix.target }}
          path: |
            scorpiofs-*.tar.gz
            scorpiofs-*.tar.gz.sha256
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: dist-*
          merge-multiple: true

      - name: Combined SHA256SUMS
        run: |
          set -euo pipefail
          cd dist
          sha256sum *.tar.gz > SHA256SUMS
          cat SHA256SUMS

      - name: Create / update GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
            dist/SHA256SUMS
          generate_release_notes: true
          fail_on_unmatched_files: true

  publish-crate:
    name: Publish to crates.io
    needs: release
    runs-on: ubuntu-latest
    # Protected environment: configure a required reviewer in GitHub so crate
    # publishing requires manual approval and is NOT triggered automatically by
    # an ordinary tag push.
    environment: crates-io
    steps:
      - uses: actions/checkout@v7

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends pkg-config libfuse3-dev libssl-dev

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: cargo publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --locked
